rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This application reads and writes Firestore through authenticated
    // server routes and Firebase Admin. The browser does not need direct data
    // access, and Admin SDK requests are not evaluated against these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
